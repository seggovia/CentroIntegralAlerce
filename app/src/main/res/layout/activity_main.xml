<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <!-- Fragment container -->
    <fragment
        android:id="@+id/nav_host"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:navGraph="@navigation/nav_graph"
        app:defaultNavHost="true"/>

    <!-- Bottom Navigation MEJORADO con paleta Alerce -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_nav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="@color/surface"
        android:elevation="8dp"
        app:itemIconTint="@color/bottom_nav_color"
        app:itemTextColor="@color/bottom_nav_color"
        app:itemIconSize="24dp"
        app:itemTextAppearanceActive="@style/Text.Caption"
        app:itemTextAppearanceInactive="@style/Text.Caption"
        app:labelVisibilityMode="labeled"
        app:itemRippleColor="@color/ripple"
        app:itemActiveIndicatorStyle="@style/BottomNav.ActiveIndicator"
        app:menu="@menu/bottom_nav_menu"/>

    <!-- FAB GLOBAL -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAddGlobal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="88dp"
        android:contentDescription="Crear actividad"
        android:src="@android:drawable/ic_input_add"
        android:visibility="gone"
        app:tint="@android:color/white"
        app:backgroundTint="@color/primary"
        app:elevation="12dp"
        app:pressedTranslationZ="16dp"
        app:rippleColor="@color/ripple"
        app:fabSize="normal"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>